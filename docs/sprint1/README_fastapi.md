# How to set up
## 1. install fastapi
<pre>
python
pip install fastapi uvicorn python-dotenv pydantic[dotenv] sqlalchemy
</pre>

## 2. some other things
-  need to decide what database to use for setup
- service running on http://localhost:5173
- health function will return ok all the time need to finish
- change the app name and database url in .env of backend
